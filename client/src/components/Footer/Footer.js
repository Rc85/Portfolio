import React, { useEffect, useState} from 'react';
import PropTypes from 'prop-types';

export const Footer = (props) => {
  return(
    <footer id='footer'>
      <div><small>rogerchin.dev</small></div>
    </footer>
  );
}

Footer.propTypes = {

}

export default Footer;